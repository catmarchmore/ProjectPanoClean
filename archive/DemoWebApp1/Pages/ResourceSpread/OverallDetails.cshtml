@page
@model ProjectPano.Pages.ResourceSpread.OverallDetailsModel
@{
    ViewData["FullWidth"] = false;
    ViewData["Title"] = "Resource Details";
}

<h5 class="custom-h1">Resource Details</h5>
<br />

<a href="/ResourceSpread/Home">Back</a>
<br/>
<br />

<a class="btn btn-primary" asp-page-handler="DownloadSpread">Export to Excel</a>
<br />
<br />

@* <table class="table table-striped table-compact">
    <thead>
        <tr>
            <th>Week Ending</th>
            <th>Group</th>
            <th>ResourceStatus</th>
            <th class="text-right">Hrs</th>
            <th>Job ID</th>
            <th>Job Name</th>
            <th>Client</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.SpreadCheckList)
        {
            <tr>
                <td>@item.WeekEnding.ToString("yyyy-MM-dd")</td>
                <td>@item.EmpResGroupDesc</td>
                <td>@item.ResourceStatus</td>
                <td class="text-right">@item.SpreadHrs</td>
                <td>@item.JobID</td>
                <td>@item.JobName</td>
                <td>@item.ClientNameShort</td>
            </tr>
        }
    </tbody>
</table> *@
